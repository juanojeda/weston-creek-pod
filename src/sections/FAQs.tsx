import React from 'react';
import { Route } from '../components/Navigation';
import { SectionWrapper } from '../components/SectionWrapper';

export const FAQs = () => {
  return (
    <SectionWrapper id={Route.FAQS}>
      FAQs
    </SectionWrapper>
  );
};